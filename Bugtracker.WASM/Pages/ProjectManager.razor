@page "/manager"

<PageTitle>Manager |Bugtracker</PageTitle>
<div class="row">
    <div class="col-2">
    </div>
    <div class="col align-self-center">
        <h1 class="oi oi-clipboard"> Project Manager</h1>
        <p class="mt-3">
            You can find here the Project Manager module. <br />
            You are able to manage every project of the website. <br />
            Also, you can display all the tickets registered in the application and assign them to the developers.
        </p>
        @if (!_isMemberConnected)
        {
            <p class="text-danger">You are not connected. Click below to go to login page</p>
            <button class="btn btn-primary" @onclick="ToAccount">Login page</button>
        }
        else
        {
            <p class="mt-5">Do you want to see the list of all projects or the list of all tickets ?</p>
            <button class="btn @((_displayProjectsComponent) ? "btn-outline-secondary" : "btn-outline-primary") mt-2 me-2" @onclick="DisplayProjectsComponent" title="All projects">All Projects</button>
            <button class="btn @((_displayTicketsComponent) ? "btn-outline-secondary" : "btn-outline-primary") mt-2" @onclick="DisplayTicketsComponent" title="All tickets">All Tickets</button>
            @if (_displayProjectsComponent)
            {
                <DisplayProjects />
            }
            else if (_displayTicketsComponent)
            {
                <DisplayTickets />
            }
        }
    </div>
    <div class="col-2">
    </div>
</div>
